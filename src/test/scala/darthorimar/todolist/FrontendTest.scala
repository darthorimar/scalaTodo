package darthorimar.todolist

object FrontendTest  {
//  private def file(filename: String): File = {
//    new File(getClass.getResource(s"../$filename").getPath)
//  }
//
//  private def testFrontend(templateFile: String, configFileOpt: Option[String], expected: String): Unit = {
//    val frontend = new Frontend(AppConfig(file(templateFile), configFileOpt.map(file), ConsoleDisplayMode))
//    val rendered = frontend.run()
//    assert(rendered.isRight)
//    assert(rendered.right.get == expected)
//  }
//
//  val tests = Tests {
//    'simpleFrontedTest - {
//      val (template, conf) = ("words.templ", None)
//      val expected =
//        """Learn new words:
//          | verbs:
//          |  go
//          |  eat
//          | nouns:
//          |  cat
//          |  kitty
//          |""".stripMargin
//      testFrontend(template, conf, expected)
//    }
//
//    'variablesTest - {
//      val (template, conf) = ("expression.templ", Option("expression.conf"))
//      val expected =
//        """42
//          |""".stripMargin
//      testFrontend(template, conf, expected)
//    }
//  }
}
